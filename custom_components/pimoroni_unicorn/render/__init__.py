"""CPython render package: device-faithful layout rendering for the HA panel.

shim.py + font8.py are the render harness (maintained here). The widget/
drawing modules are auto-generated copies of firmware/ — see
scripts/sync_render.py. render_service.py drives them.
"""
