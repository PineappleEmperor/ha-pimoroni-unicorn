{
  "name": "pimoroni-unicorn-panel",
  "version": "1.0.0",
  "private": true,
  "description": "Layout editor panel for the Pimoroni Unicorn Home Assistant integration",
  "type": "module",
  "scripts": {
    "build": "esbuild src/panel.ts --bundle --format=esm --minify --outfile=../custom_components/pimoroni_unicorn/panel/editor.js",
    "check": "tsc --noEmit"
  },
  "dependencies": {
    "lit": "^3.1.0"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "typescript": "^5.4.0"
  }
}
